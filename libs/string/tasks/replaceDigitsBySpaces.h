#ifndef INC_REPLACE_DIGITS_BY_SPACES_H
#define INC_REPLACE_DIGITS_BY_SPACES_H

#include "../string_.h"

// преобразовывает строку s, заменяя каждую цифру соответствующим
// ей числом пробелов.
void replaceDigitsBySpaces(char *s);

#endif
